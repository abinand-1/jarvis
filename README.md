# jarvis

clone the url using
git clone git@github.com:abinand-1/jarvis.git
cd jarvis && source venc/bin/active
the project used python 3.11 version
jarvis_daemon is used as the main file. currently working on open native apps. on browser apps lauch
